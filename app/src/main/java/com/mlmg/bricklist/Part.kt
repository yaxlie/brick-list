package com.mlmg.bricklist

class Part(id:Int, typeId:Int, code:String, name:String, namePl:String?, categoryId:Int) {
    var id = id
    var typeId = typeId
    var code = code
    var name = name
    var namePl = namePl
    var categoryId = categoryId
}